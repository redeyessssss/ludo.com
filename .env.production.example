# Production Environment Variables

# ============================================
# VERCEL (Frontend) Environment Variables
# ============================================
# Add these in Vercel Dashboard → Settings → Environment Variables

# Backend API URL (Your Render backend URL)
VITE_API_URL=https://ludo-backend.onrender.com


# ============================================
# RENDER (Backend) Environment Variables
# ============================================
# Add these in Render Dashboard → Environment

# Frontend URL (Your Vercel frontend URL)
CLIENT_URL=https://ludo-com.vercel.app

# JWT Secret (Generate a random string)
JWT_SECRET=change-this-to-a-random-secret-key

# Node Environment
NODE_ENV=production

# Port (Render will override this)
PORT=3001


# ============================================
# IMPORTANT NOTES
# ============================================
# 1. Replace URLs with your actual deployment URLs
# 2. Remove trailing slashes from URLs
# 3. Use https:// not http://
# 4. Generate a strong JWT_SECRET (use: openssl rand -base64 32)
# 5. Never commit .env files to Git (only .env.example files)
